<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Account -->
    <style name="AccountVerifyTheme" parent="AppTheme.Arrow">
        <item name="walletPrimaryColor">@color/account_primary_color</item>
    </style>

    <!-- Setting -->
    <style name="SettingTheme" parent="AppTheme.Arrow" />

    <style name="SettingWebTheme" parent="AppTheme.Arrow" />

    <style name="LepayEntryTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- 我的乐视主题 -->
    <style name="LeEcoTheme" parent="AppTheme.WebView">
        <item name="android:textAppearance">@style/TextAppearance.LeEcoActionBarTextAppearance</item>
    </style>
</resources>